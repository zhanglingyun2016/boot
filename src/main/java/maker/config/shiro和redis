shiro和redis：


---------------------------------------------------
登录逻辑：
1.用户在前端输入用户名和密码，将传入后端，进行校验用户名和密码，
    1）校验成功了将生成Jwt，将返回用户
            用户访问后端受限资源，调用其他的接口，将要携带身份凭证Jwt，进行访问
    2）校验失败了将抛出异常









